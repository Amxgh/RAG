version: "3.9"

services:
  waam-rag:
    build: .
    container_name: waam-rag
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./papers:/app/papers
      - ./config:/app/config
    restart: unless-stopped